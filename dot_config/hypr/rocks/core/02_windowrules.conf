# ════════════════════════════════════════════════════════════════
#  GÖRSEL KURALLAR — Pencere Düzeni, Dekorasyon, Animasyonlar
#  3×3 Grid çalışma alanı düzeni burada tanımlanır.
# ════════════════════════════════════════════════════════════════

# ─── GENEL PENCERE DÜZENİ ───────────────────────────────────────
general {
    gaps_in          = 5
    gaps_out         = 20
    border_size      = 2
    resize_on_border = true
    allow_tearing    = true
    layout           = dwindle
}

# ─── TILING MOTORU (DWINDLE) ────────────────────────────────────
dwindle {
    preserve_split        = true
    smart_split           = true
    smart_resizing        = true
    split_width_multiplier = 1.0
    use_active_for_splits = true
    precise_mouse_move    = true
}

# ─── DEKORASYON (Blur, Gölge, Şeffaflık) ───────────────────────
decoration {
    rounding       = 0
    rounding_power = 2

    active_opacity   = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
		color = rgba(242424ee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1
        vibrancy = 0.1696
    }

}

animations {
    enabled = yes,
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

dwindle {
    pseudotile = true
    preserve_split = true 
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = -1 
    disable_hyprland_logo = false
}
#
# windowrule = workspace special:dashboard, class:^(dashboard)$
#
# # ══════════════════════════════════════════════════════════════════════════════
# # WORKSPACE BOUNCER - 3x3 Grid Protection (Workspaces 1-9 ONLY)
# # ══════════════════════════════════════════════════════════════════════════════
# # If ANY window lands on workspace 10+, immediately redirect to workspace 9
# # This catches edge cases from session restore, manual workspace switches, or bugs
# # Source Analysis: windowrulev2 is applied AFTER plugin's onWindowOpen hook,
# # but stickyrule (plugin level) is stronger for initial placement
# windowrule = workspace 9, workspace:^(1[0-9]|[2-9][0-9])$
# windowrule = workspace 9, workspace:^([1-9][0-9]{2,})$
